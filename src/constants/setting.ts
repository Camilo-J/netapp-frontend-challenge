const BASE_URI = import.meta.env.VITE_BASE_URI as string;
const tokenKey = import.meta.env.VITE_TOKEN_KEY as string;
const PEXEL_KEY = import.meta.env.VITE_PEXELS_KEY as string;
const PEXEL_URL = import.meta.env.VITE_PEXELS_URL as string;

export { BASE_URI, tokenKey, PEXEL_KEY, PEXEL_URL };
